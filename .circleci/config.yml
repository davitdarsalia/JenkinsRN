version: 2.1

jobs:
  checkout-code:
    docker:
      - image: cimg/base:current

    steps:
      - checkout
      - run:
          name: Latest commit from Main branch
          command: git pull && git fetch

workflows:
  android:
    jobs:
      - checkout-code
